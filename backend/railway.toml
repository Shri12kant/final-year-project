[build]
builder = "NIXPACKS"

[deploy]
startCommand = "mvn clean package && java -jar target/*.jar"

[mounts]
# Mount volume for persistent file storage
mountPath = "/app/uploads"
name = "uploads-volume"
